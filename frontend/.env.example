# URL do backend (usada pelo proxy do Vite em desenvolvimento)
# Em produção, o nginx faz o proxy — esta variável não tem efeito no build
BACKEND_URL=http://127.0.0.1:8000

# Porta do servidor de desenvolvimento Vite (padrão: 5173)
PORT=5173
